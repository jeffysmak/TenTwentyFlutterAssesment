import 'package:flutter/material.dart';

class ScreenLibrary extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Screen Library')),
    );
  }
}
